// The Twitter/X card reuses the OpenGraph card — same 1200×630 artwork, but
// emitted as `twitter:image` so clients that ignore og:image still get one.
export { alt, size, contentType, default } from "./opengraph-image";
